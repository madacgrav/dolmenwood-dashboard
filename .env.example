# Firebase Configuration (REQUIRED for cloud sync)

# Get these values from your Firebase Console:
# 1. Go to https://console.firebase.google.com/
# 2. Create a new project (or use existing)
# 3. Click "Add app" → Web app
# 4. Copy the config values below

VITE_FIREBASE_API_KEY=your-api-key-here
VITE_FIREBASE_AUTH_DOMAIN=your-project.firebaseapp.com
VITE_FIREBASE_PROJECT_ID=your-project-id
VITE_FIREBASE_STORAGE_BUCKET=your-project.firebasestorage.app
VITE_FIREBASE_MESSAGING_SENDER_ID=123456789012
VITE_FIREBASE_APP_ID=1:123456789012:web:abcdef123456

# Note: These are public configuration values and are safe to commit
# Firebase security is handled by Firestore Security Rules

# Backend API Configuration (OPTIONAL - for Ask a Question feature with real GitHub Copilot)

# Backend server port (default: 3001)
PORT=3001

# CORS origin - the frontend URL that can access the backend
# For development: http://localhost:3000
# For production: your deployed frontend URL
CORS_ORIGIN=http://localhost:3000

# GitHub Copilot Integration (REQUIRED for backend API proxy)
# SECURITY WARNING: NEVER expose this token in client-side code or commit it to version control
# Get your token from: https://github.com/settings/tokens
# Required scopes: copilot
GITHUB_TOKEN=your-github-personal-access-token-here

# Copilot agent name (default: dolmenwood)
COPILOT_AGENT_NAME=dolmenwood

# Frontend API proxy URL (for frontend to connect to backend)
# This should point to your backend server endpoint
VITE_GITHUB_API_PROXY_URL=http://localhost:3001/api/copilot/ask

# Node environment (development or production)
NODE_ENV=development

# Security Note:
# GitHub authentication tokens should NEVER be stored in environment variables
# accessible to the client (variables without VITE_ prefix are backend-only).
# The backend proxy handles secure authentication with GitHub.
