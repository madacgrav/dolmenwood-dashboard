# Firebase Configuration (REQUIRED for cloud sync)

# Get these values from your Firebase Console:
# 1. Go to https://console.firebase.google.com/
# 2. Create a new project (or use existing)
# 3. Click "Add app" → Web app
# 4. Copy the config values below

VITE_FIREBASE_API_KEY=your-api-key-here
VITE_FIREBASE_AUTH_DOMAIN=your-project.firebaseapp.com
VITE_FIREBASE_PROJECT_ID=your-project-id
VITE_FIREBASE_STORAGE_BUCKET=your-project.firebasestorage.app
VITE_FIREBASE_MESSAGING_SENDER_ID=123456789012
VITE_FIREBASE_APP_ID=1:123456789012:web:abcdef123456

# Note: These are public configuration values and are safe to commit
# Firebase security is handled by Firestore Security Rules

# GitHub Copilot Integration (OPTIONAL - for Ask a Question feature)

# To enable real GitHub Copilot custom agent integration:
# 1. Create a backend API proxy to securely handle GitHub authentication
# 2. Configure the proxy endpoint below
# 3. The proxy should authenticate with GitHub and forward requests to the Copilot API
# Note: Never expose GitHub API keys or tokens in client-side code

# VITE_GITHUB_API_PROXY_URL=https://your-backend-api.com/copilot/ask
# VITE_COPILOT_AGENT_NAME=dolmenwood

# Security Note:
# GitHub authentication tokens should NEVER be stored in environment variables
# accessible to the client. Use a secure backend proxy for production deployments.
